# AWS Configuration
AWS_REGION=us-east-1
AWS_ACCOUNT_ID=your-account-id

# Gateway Configuration
GATEWAY_ID=your-gateway-id
GATEWAY_NAME=your-mcp-gateway
MCP_SERVER_URL=https://your-mcp-server.example.com/mcp

# OAuth Configuration (stored in AWS Parameter Store)
# These are the Parameter Store paths - do not put actual values here
OAUTH_CLIENT_ID_PATH=/infragenie/oauth/client_id
OAUTH_CLIENT_SECRET_PATH=/infragenie/oauth/client_secret
OAUTH_ISSUER_URL_PATH=/infragenie/oauth/issuer_url
OAUTH_AUDIENCE_PATH=/infragenie/oauth/audience

# For local testing only (not recommended for production)
# Uncomment and set these if you want to test without Parameter Store
# ANSIBLE_MCP_CLIENT_ID=your-client-id
# ANSIBLE_MCP_CLIENT_SECRET=your-client-secret
# ANSIBLE_MCP_ISSUER_URL=https://your-auth-provider.com/
# ANSIBLE_MCP_AUDIENCE=your-api-audience
